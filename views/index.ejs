<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>오늘의 점심 참여</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body>
    <main class="container">
      <header>
        <h1>오늘의 점심</h1>
        <p class="sub">날짜: <strong><%= today %></strong></p>
      </header>

      <section class="signup">
        <h2>참여하기</h2>
        <form method="post" action="/signup" class="row">
          <input type="text" name="name" placeholder="이름" required maxlength="100" />
          <button type="submit">참여</button>
        </form>
        <details class="mt">
          <summary>내 이름 삭제하기</summary>
          <form method="post" action="/remove" class="row mt-sm">
            <input type="text" name="name" placeholder="이름" required maxlength="100" />
            <button type="submit" class="secondary">삭제</button>
          </form>
        </details>
      </section>

      <section class="list">
        <h2>오늘 참여자 (<%= signups.length %>)</h2>
        <% if (signups.length === 0) { %>
          <p class="muted">아직 참여자가 없습니다. 먼저 참여해보세요!</p>
        <% } else { %>
          <ol class="names">
            <% signups.forEach(s => { %>
              <li>
                <span class="name"><%= s.name %></span>
              </li>
            <% }) %>
          </ol>
        <% } %>
      </section>

      <footer>
        <p class="muted">KAPCQ 퀸즈장로교회 교직원 </p>
      </footer>
    </main>
  </body>
  </html>


